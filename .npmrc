# GitHub Packages registry for @summits scope
@summits:registry=https://npm.pkg.github.com

# Authentication (use environment variable or local ~/.npmrc)
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
